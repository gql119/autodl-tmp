seed: 0
stage: L3
artifact_root: artifacts/oa_lgc/local
num_classes: 20
model:
  pool_size: 4
  hidden_dim: 16
virtual_update:
  steps: [1, 3, 5]
  learning_rate: 0.1
  mode: head_only
  first_order: true
  selected_modules: [feature_proj.]

