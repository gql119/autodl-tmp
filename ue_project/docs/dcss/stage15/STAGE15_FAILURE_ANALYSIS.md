# Stage 1.5 失败分析

状态：completed。metadata不足使0.8735独立性不可证明；100 epoch clean baseline underfitting；M3存在63.3% energy/leak梯度冲突；M4发生target/leakage trade-off；M6确认Legacy carrier coupling；M6相对random仍selectivity fail。所有run隔离，不影响Stage 0/1/1R。
