## PRERUN-REVIEW-01

- Result: blocked
- Decision: do_not_run
- Gated run: paired sparse E20 only; reuse the verified P1 state, build C0/M1 mixed path lists, train fresh C0 then fresh M1 for 20 epochs, evaluate clean VOC20, compare, and shut down. No mechanism, smoke, EOT, robustness, 200-epoch, resume, or old method run is permitted.
- Code snapshot: branch `codex/tausb-sdh-lfc-cicr-cgr-nla-map50-v3`, execution commit `b70fc87ecfcda8c2adb5f40b86a1147dbe738633`; the GitHub branch resolves to the same SHA. The later review-metadata commit is not an execution input.
- Intent: remove redundant clean-image copies without changing the fixed secret, frozen P1 renderer, target support, losses, victim protocol, clean validation, or AP50 computation.
- Code location: `ue_framework/sparse_dataset.py`; `stages/generate.py`; `stages/train_victim.py`; `stages/evaluate.py`; `tools/bind_tausb_sdh_e2e_v0.py`; `tools/run_tausb_sdh_sparse_e20.py`; shutdown wrapper `sparse_e20_controller.sh`.
- Parameter data flow: exact controller CLI -> E20-only binder -> frozen P1 hash validation -> `materialization_layout=sparse_mixed_list_v1` -> C0/M1 manifest and absolute train list -> full manifest/hash/label audit -> real Ultralytics dataloader probe -> fresh `YOLO(config).train` -> clean VOC val -> named 20-class `metrics.json` -> paired comparison.
- Runtime state: C0 initializes no surrogate or poison generator and references 16,551 original JPEGs. M1 initializes the existing `tausb_sdh` frozen-state generator and saves exactly 6,095 person-image PNGs; 10,456 person-free images remain original JPEG references. Victim `resume=false`, epochs=20, imgsz=640, batch=36, optimizer=SGD, seed=0. Periodic bundles are disabled.
- Sink effect: the change reaches the active victim `data=` YAML through an explicit train TXT and reaches the metrics sink through sparse list/stem/label hashes and saved-reload quality values. Carrier, D-LFC, CICR, CGR, NLA, TAL, target class, model architecture, optimizer, and AP50 code are unchanged.
- Baseline/disable path: C0 is a separately initialized clean-data victim; legacy/full-PNG configs remain accepted and keep directory-based `images/train` behavior. Smoke remains `full_png_v1`. No fallback to hardlinks, `tausb_mask`, old poisoned roots, or full-image rewrite exists in sparse mode.
- Local validation: 92 focused SDH tests passed; Python 3.12 compile and Python 3.8 AST checks passed; controller CLI and Bash syntax passed; diff and credential scans passed. Real local VOC audit found 16,551 images, 6,095 person images, no missing/corrupt labels, and a real Ultralytics 8.4.95 batch. Sixty-four JPEG decode/PNG round trips were pixel exact. The C0 all-zero AP50 gate prevents M1 training.
- Minimal probe: full real-VOC C0 mixed list hash `6dfe46162c44ed809f52374c50aca288c953e63098ad4611d90a582fa0863c6b`; ordered-stem hash `2e0f30546c8848b7f2b9c4239b49ff417dba3d51e2bd54b354fb0f299ea00011`; label-manifest hash `022fbdace84899bf5d340cd07f2eb1d51834c3d0bd35b446f4fef11eb2a53216`.
- Run command binding: planned tmux session `tausb-sdh-e2e-v0-sparse-e20-s0-r1`; planned clean detached checkout `/root/tausb-sdh-checkouts/e2e-v0-sparse-b70fc87-worktree`; wrapper must be invoked with `EXPECTED_COMMIT=b70fc87ecfcda8c2adb5f40b86a1147dbe738633`. The exact environment/root command remains gated until the read-only remote audit confirms every path is present or fresh.
- Experiment validity: VOC2007+2012 train 16,551; target `person` id 14; C0=0 and M1=6,095 poisoned images; independent fresh victims; seed0; E20; clean validation only; 20 named per-class AP50 values plus 19-class macro and retention. This is single-seed feasibility evidence only.
- Output non-overwrite: controller refuses any pre-existing binding, control, log, comparison, C0, or M1 root. The exact remote roots have not yet been observed because the instance is offline, so launch is forbidden.
- Recoverability/secrecy: one tmux controller, atomic controller status, separate stage logs, two-hour overall cap, 40-minute materialization/arm caps, ten-minute idle guard, explicit C0 interpretability gate, and wrapper shutdown on every terminal state. No credential, SSH endpoint, token, dataset, weight, or P1 binary is stored in the command packet or commit.
- Blockers: the authorized AutoDL SSH endpoint returned `Connection refused` on 2026-08-12. A live read-only audit must verify exact checkout, Python/Ultralytics/CUDA, P1/config/checkpoint/data hashes and counts, at least 6,948,745,251 free bytes on the run filesystem, idle GPU, absent tmux session, fresh roots, and executable `tmux`/`shutdown`. Until then the decision is `do_not_run`.
- Validation gaps: real remote Ultralytics 8.4.33 sparse-list batch, real saved M1 PNG count/hash/Linf/support audit, wall-clock cost, GPU health, victim AP50, comparison metrics, and shutdown confirmation.
