## PRERUN-REVIEW-01

- Result: pass
- Decision: allow_run
- Gated run: only `SPARSE-E200-S0-R1`, using the exact contract in `sparse_e200_run_contract.json`. Authorization does not extend to a different commit, P1, dataset, output root, resume run, or method variant.
- Code snapshot: branch `codex/tausb-sdh-lfc-cicr-cgr-nla-map50-v3`; execution commit `36f74cab2222f41cb1f206b42db3118237f18a52`; the GitHub branch resolved to the same SHA after an ordinary non-force push. Wrapper SHA256 is `06a7f652fbb12d4a0c9bd896ca836a063b05a4b9d826cd64abd25548287c3684`; controller SHA256 is `33369482c5e4595804bd9f481c75bac827476926a8c6a11af073410179090d72`.
- Intent: run one matched sparse C0/M1 full-horizon E200 experiment to determine whether the E20 person shortcut persists and whether the 19 non-target classes recover, remain impaired, or deteriorate. The method, secret carrier, P1, support, perturbation budget, data split, victim optimizer, and clean metric are frozen.
- Code location: E200 selection and orchestration are active in `run_tausb_sdh_sparse_e20.py`; config binding is active in `bind_tausb_sdh_e2e_v0.py`; fresh victim construction and pre-train hash evidence are active in `train_victim.py`; clean VOC20 AP50 is active in `evaluate.py`; outer timeout/evidence flush/shutdown are active in `sparse_e200_controller_data_disk_v1.sh`.
- Parameter data flow: CLI `--victim-epochs 200` -> E200 contract -> binder `--full-voc-only --victim-epochs 200` -> `e200-c0.yaml` / `e200-m1.yaml` with `victim.epochs=200` -> `RunContext` -> `train_victim.py` `train_args[epochs]` -> Ultralytics `model.train(epochs=200)` -> independent clean VOC validation -> 20 named AP50 values -> paired comparison and scientific classification.
- Runtime state: both victims are YOLOv8n-style VOC20 models built from `configs/voc_yolov8n_20cls.yaml`, fresh and no-resume, seed 0, imgsz 640, batch 36, SGD. The frozen YOLOv8n surrogate is read-only and is rejected unless SHA256 equals `8de8a0c78c6414ad0bf98052b3bc96c33d8e854a2a2a905d47c8195363975b89`; it is not used to initialize either victim. Remote GPU visibility and idleness are fail-closed prechecks before any output stage.
- Sink effect: local construction produced a canonical YOLOv8n init hash, and the real local VOC loader consumed a labeled batch of shape `[1,3,480,640]`. The E200 controller injects C0's initialization hash before M1 model construction; M1 refuses to call `model.train` if the hash differs. C0 must also pass person AP50 `>=0.60` and non-target macro AP50 `>=0.50` before M1 starts.
- Baseline/disable path: C0 references all 16,551 original JPEGs and has `poisoned_count=0`; M1 materializes only the 6,095 person images as lossless PNG and references the remaining 10,456 original JPEGs. The E20 contract remains separately selectable at 20 epochs and its prior artifacts are read-only. No robustness transform is enabled for the main clean metric.
- Local validation: all 117 SDH/fresh-victim focused and regression tests passed; Python 3.8 AST parsing passed for six runtime modules; Git Bash `bash -n` passed; scoped diff checks passed. The exact E200 P1 and frozen-state hashes now have a dedicated fail-closed gate and regression test.
- Minimal probe: the real local VOC probe used image SHA256 `fbb7fad63f242bf15fef88a2532b362241586cba4bf9dd0f26c09b94e42c5659`, label SHA256 `9d68a888d5b0b628888e0266768cfdb07594fcff593883803954043a2917e42f`, and list SHA256 `96e59ac444d086653587df889c0188c79e525aabe5a2c10477548390179c52ac`. This verifies the active loader path without claiming E200 effectiveness.
- Run command binding: tmux session `tausb-sdh-e200-s0-r1`; detached clean checkout `/root/autodl-tmp/tausb-sdh/checkouts/36f74cab2222f41cb1f206b42db3118237f18a52`; exact environment, paths, hashes and wrapper are frozen in `sparse_e200_run_contract.json`. Only the reviewed wrapper may invoke the controller.
- Experiment validity: VOC2007+2012 train 16,551; target person id 14; M1 poison count 6,095; clean VOC validation; 20 class AP50; seed 0; fresh paired E200; perturbation budget 16/255; single-seed, same-architecture white-box claim boundary. Success requires person drop `>=0.30`, non-target macro drop `<=0.05`, and at least 16/19 non-target drops `<=0.10`. Failure and inconclusive outcomes are equally retained and reported.
- Output non-overwrite: checkout, binding, C0/M1, control, log and comparison paths are unique to `SPARSE-E200-S0-R1`; the controller refuses any pre-existing fresh output root and refuses a dirty checkout. It never enables force-resume and cannot overwrite E20 artifacts.
- Recoverability/secrecy: a persistent tmux session is used; the controller emits atomic stage state and per-stage logs. C0 and M1 each have a 12,600-second train+evaluate cap, materialization has 1,200 seconds, inactivity has 600 seconds, and an independent GNU timeout enforces the total 32,400-second hard cap. Traceback, CUDA OOM, NaN and Inf log signals terminate the stage. Every success, scientific failure, inconclusive result, operational failure and timeout writes a SHA256 terminal evidence manifest before the shutdown trap calls `shutdown -h now`. No credential is stored in the contract, command or repository.
- Blockers: none in the reviewed implementation. `allow_run` is conditional on the controller's live remote prechecks; any failed live precheck means no training and automatic shutdown.
- Validation gaps: AutoDL's current GPU identity/idleness, `/root/autodl-tmp` device, current free bytes, system-disk growth, frozen input file presence, and actual output-root absence cannot be established from Windows. They are deliberately checked on the remote host before binding or training. E200 wall time, finite training losses, 200-epoch completion, AP50 values, scientific outcome, evidence pull and shutdown confirmation remain experiment outputs and are not inferred from this review.
